#include <stdio.h>

/**
 * main - prints all single digit numbers of base
 * and a new line
 * Return: 0
 */

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
